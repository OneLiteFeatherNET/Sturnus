// Nuxt generates the bulk of this configuration from the project itself --
// which files are components, which globals are auto-imported, which
// directory layout applies. Extending the generated config rather than
// restating it is what keeps the rules from drifting out of step with the
// framework version.
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt()
